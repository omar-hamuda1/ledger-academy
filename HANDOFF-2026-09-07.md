# Ledger Academy — Session Handoff (2026-09-07)

Single-file snapshot so work can resume cleanly in a new session. For deep
architectural/feature detail see **`PROJECT_STATE.md`**; for the rules that keep
changes from regressing see **`CLAUDE.md`**. This file is the "where are we,
what's next" layer on top of those.

---

## 1. What the project is

**Ledger Academy** — Arabic (RTL), dark-navy + gold e-learning platform teaching
**Business Administration (إدارة الأعمال)** to Egyptian secondary-school students
(3 grade levels). Lecturer/owner = "محمد حسين" (`ADMIN` role). Hard rule: the word
**"مدرس" must never appear in the UI** — use "محاضر" / "مدرب".

- **Stack**: Next.js 16 (App Router) + React 19 + TypeScript, Tailwind v4 (CSS
  `@theme`, no config file), Prisma v6 + Postgres on **Neon**, NextAuth v4
  (credentials, JWT, roles `ADMIN`/`STUDENT`), shadcn/ui remapped to navy/gold,
  framer-motion, Recharts, sonner.
- **~11,500 lines** of hand-written code (`src` 9.2k, `prisma` 0.4k, `tests` 0.9k).
- **No online payment.** Stripe was removed 2026-09-07 (no Egypt merchant
  support). Paid courses unlock via a **prepaid code** or an **admin-approved
  `CodeOrder`** (student uploads a payment-proof screenshot → admin approves →
  code auto-issued + enrollment created → student notified). Money moves manually
  (Vodafone Cash / InstaPay / bank / cash). `price` is a display figure in EGP.

---

## 2. Deployment status — LIVE (private staging)

| Thing | Value |
| --- | --- |
| **Live URL** | https://ledger-academy-liart.vercel.app |
| Host | Vercel (Hobby plan), region pinned `fra1` (Frankfurt, near the Neon `eu-central-1` DB) |
| Repo | https://github.com/omar-hamuda1/ledger-academy — Vercel auto-deploys on push to `main` |
| Deployed commit | `e6cd486` "Prep for Vercel deploy (private staging)" |
| Database | Neon project `late-tree-77912979`, **production** branch `br-super-scene-b1evrezh` (the deployed site uses this) |
| Test branch | `br-falling-thunder-b19d3iba` → `.env.test` (Vitest only, copy-on-write off prod) |
| Build command | `prisma generate && next build` (set in `package.json`) |
| `vercel.json` | `{ "regions": ["fra1"] }` |

**Health check (last run 2026-09-07, all good):**
`/` 200 · `/login` 200 · `/courses` 200 · `/api/notifications` 401 (correct, needs
auth) · `/api/auth/csrf` 200.

> ✅ **Privacy gate LIVE (2026-09-07).** `src/middleware.ts` has a `STAGING_PASSWORD`
> gate; the env var is set on Vercel and verified live (`/` → Arabic password
> page, `/api/*` → 401, `X-Robots-Tag: noindex` on every response). Unlock is a
> one-time password submit → httpOnly cookie, 30 d. **Unset `STAGING_PASSWORD` +
> redeploy to open the site for launch.**

### Environment variables set on Vercel (all environments)

| Var | Status | Notes |
| --- | --- | --- |
| `DATABASE_URL` | ✅ set | Neon **pooled** string (`-pooler` host). Was once pasted with quotes — fixed. |
| `NEXTAUTH_SECRET` | ✅ set | Copied from local `.env.local`. |
| `NEXTAUTH_URL` | ✅ set | `https://ledger-academy-liart.vercel.app`. Was briefly wrong (`ledger-academy.vercel.app` — that subdomain is an unrelated app, name collision) — corrected + redeployed + verified. |
| `AWS_ACCESS_KEY_ID` | ✅ set (2026-09-07) | Neon Object Storage, prod branch. |
| `AWS_SECRET_ACCESS_KEY` | ✅ set | ″ |
| `AWS_ENDPOINT_URL_S3` | ✅ set | `https://br-super-scene-b1evrezh.storage.c-5.eu-central-1.aws.neon.tech` |
| `AWS_REGION` | ✅ set | `eu-central-1` |
| `UPSTASH_REDIS_REST_URL` | ✅ set (2026-09-07) | `https://robust-locust-79999.upstash.io` — free Upstash Redis DB `ledger-academy`. |
| `UPSTASH_REDIS_REST_TOKEN` | ✅ set | Secret; rotate in Upstash console if ever leaked. |
| `BREVO_API_KEY` / `EMAIL_FROM` / `EMAIL_FROM_NAME` | ✅ set (Production, 2026-09-07) | `EMAIL_FROM=mohamed.hussien.academy@gmail.com`, `EMAIL_FROM_NAME=Mohamed Hussien`. Live site reaches Brevo fine, but **Brevo hasn't activated transactional sending on the new account** ("sending platform is currently disabled") → signup / password reset still don't deliver. Owner to open a Brevo support ticket. See §4.3. |

Redeployed after adding the AWS + Upstash vars. **Next real test is the
end-to-end payment-proof flow** (§4).

---

## 3. Login credentials (production DB)

| Email | Role | Password |
| --- | --- | --- |
| `instructor@example.com` | ADMIN | `password123` (from `prisma/seed.ts`) |
| `omarhamuda20@gmail.com` | ADMIN | whatever the owner set (promoted from STUDENT, password untouched) |
| `omar@gmail.com` | STUDENT | owner knows |

Break-glass if locked out (run locally, `.env` points at prod):
```
npm run admin:recover -- --email you@example.com --password "NewPass123"
```

---

## 4. What's LEFT to do (in priority order)

### 4.1 End-to-end payment-proof smoke test on the live site  ✅ DONE (verified 2026-09-07 ~13:00)
Confirmed complete on the prod branch by inspecting live data + storage (not a
fresh run — the owner/an earlier pass already exercised it):
- `CodeOrder` `cmtqyj0ti…` is `APPROVED`, `paymentProofKey =
  proofs/cmtp9lxmq…/fd3d6065-….jpg` — and that object (562 KB JPG) **physically
  exists in the private `payment-proofs` bucket** → the `AWS_*` storage vars work
  (the upload route did not 503).
- The approve transaction issued `PrepaidCode` `cmtqyjqw8…` (`isUsed = true`,
  `usedAt` set) **and** created the enrollment — all 3 paid courses now have 1
  enrollment each.
- 5 broadcast `Notification` rows + 4 `NotificationRead` rows on prod → the bell
  works live. `/`, `/courses`, `/pricing`, `/api/auth/csrf` 200; `/api/notifications`
  401 (correct).

Original steps, kept for reference — this is the one flow that exercises the
newly-added `AWS_*` + Upstash vars.

1. **Admin** (`instructor@example.com`): open `/dashboard/admin` — confirm charts
   render, notification bell doesn't error. Create a course with **price > 0**,
   publish it.
2. **Student** (`omar@gmail.com` or a throwaway): open that paid course → see the
   **"اطلب كودًا"** / `RequestAccessForm` (phone + payment note + **attach a
   screenshot image**, JPG/PNG ≤ 5 MB) → submit. Must succeed with an Arabic
   confirmation. **If it 503s → the `AWS_*` vars are wrong** (this is the real
   storage test).
3. **Admin** → `/dashboard/admin/code-orders` → the request is there with a
   **"عرض الإيصال"** link that opens the uploaded screenshot (1 h presigned URL) →
   click **موافقة (approve)**. A `PrepaidCode` is auto-issued + `Enrollment`
   created in one transaction; student is `notify()`d.
4. **Student** → bell dropdown shows the notification with the code → enter it in
   **"أدخل كود الكورس"** → course unlocks, lessons accessible.
5. Also sanity-check: `/dashboard/student` renders (rings, gamification widget,
   announcement banner if active), `/courses` + `/pricing` + `/` still fine.

Report where it breaks; the storage wiring lives in `src/lib/storage.ts` +
`src/app/api/uploads/payment-proof/route.ts` + `src/app/api/code-orders/*`.

### 4.2 Verify the notification schema on the `test` branch + re-run tests  ✅ DONE (2026-09-07 ~13:00)
`prisma db push` against the `test` branch reported **"already in sync"** — the
unified `Notification` / `NotificationRead` model is present there (no data loss,
nothing dropped). Full `vitest run`: **10 files / 27 tests pass**, including
`tests/api/notifications.test.ts` (3 cases — first run ever),
`prepaid-code-redeem.test.ts` (6), and `code-orders.test.ts` (7).

### 4.3 Brevo activation (blocks real signups)  ← BLOCKED on Brevo support, everything else done
Full setup done 2026-09-07 and **verified working up to Brevo's own gate**:
- `src/lib/email.ts` calls **Brevo**'s transactional HTTP API (switched from
  SendGrid — 300 emails/day free, no card). Deployed: commit `9a09eee`, Production.
- Brevo account created. Two **verified** senders:
  `mohamed.hussien.academy@gmail.com` (the one in use) and `omarhamuda20@gmail.com`.
- `BREVO_API_KEY` + `EMAIL_FROM` (`mohamed.hussien.academy@gmail.com`) +
  `EMAIL_FROM_NAME` (`Mohamed Hussien`) set in `.env.local` **and** on Vercel
  (Production), redeployed.
- Proven end-to-end: a live `POST /api/auth/otp/send` reaches Brevo's API with the
  right key + sender (shows in Brevo → Transactional → Logs). One early test email
  (before a burst of retries) **delivered to Gmail**.

**The one remaining blocker:** Brevo shows *"Your sending platform is currently
disabled"* on the transactional log. On a **new Brevo account, transactional
sending must be activated manually by Brevo support** — it is not a config error
on our side (verified sender, valid key, deployed code, correct env vars all
confirmed). The burst of test sends likely tripped their new-account anti-abuse
throttle on top of that.

**To finish (owner):** open a Brevo support ticket (**? → Contact us**, or
help.brevo.com/hc/en-us/requests/new) asking them to **activate transactional
email** for the account — mention it's low-volume OTP codes (<100/day) for an
e-learning platform, verified sender `mohamed.hussien.academy@gmail.com`. Once
they flip it on, email works with **no further code/env changes**. Re-test:
`POST /api/auth/otp/send` (or `/forgot-password`) → check Brevo Logs for
"Delivered". Note the freemail-sender warning still stands — a custom domain +
Brevo domain auth is the right pre-launch move for inbox delivery.

**Fallback if Brevo stays stuck / gets declined:** Gmail SMTP via `nodemailer`
(send through `mohamed.hussien.academy@gmail.com` directly — needs a Google App
Password; ~15 min code swap). Not done — owner chose to wait on Brevo instead
(2026-09-07). Until email works, use `admin:recover` or the dev OTP log to get
accounts in.

### 4.4 Smaller open items (from `PROJECT_STATE.md` §4)
- Prepaid codes: no revoke/delete or CSV export.
- Code-order **new-request** alerts: DONE 2026-09-07 — sound + desktop
  notification in the bell, plus email (`ADMIN_ALERT_EMAILS` / ADMIN emails, via
  Brevo) and Telegram (`TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID`) sent from
  `POST /api/code-orders` inside `after()`. Email waits on Brevo activation;
  Telegram needs the owner to make a bot + set the two env vars on Vercel.
- Code-order **approve/reject**: still in-app `notify()` only — no email/Telegram
  to the student on approve/reject; no student self-cancel of a PENDING request.
- ~~No user role-management UI~~ — DONE 2026-09-07 (promote/demote + disable/enable
  at `/dashboard/admin/users`; needs `prisma db push` to prod for `User.disabledAt`).
- ~~No module/lesson reordering UI~~ — DONE 2026-09-07 (▲/▼ move controls).
- Lesson-player desktop double-sidebar (cosmetic; mobile fixed in UI Phase 3).
- No real imagery — `public/` still has default Next.js SVGs; no course
  thumbnails, no instructor photo. Needs owner-supplied files.
- All lesson `videoUrl`s point at a 15 s Google sample clip — needs real footage.
  (Videos are meant to be YouTube "Unlisted" links — see `CLAUDE.md` "Lesson
  videos".)
- Real device / screen-reader / mobile-viewport testing of the UI overhaul
  (Phases 1–3) still not done — no browser tool in those sessions.
- Staging privacy gate: code done (§2) — just need to set `STAGING_PASSWORD` on
  Vercel + redeploy.

### 4.5 Not launched — gating items before going public
Real course content · real videos · Brevo email live · instructor photo + course
thumbnails · full end-to-end pass on the live site · custom-domain decision (also
fixes email deliverability) · unset `STAGING_PASSWORD` when going public · lawyer
review of `/privacy` + `/terms` (minors' data).

---

## 5. Recent work this session (newest first)

All committed + pushed to `origin/main`. Recent commits:

| Commit | What |
| --- | --- |
| `e6cd486` | Prep for Vercel deploy — serverless-safe Prisma singleton (`src/lib/db.ts`), `build` = `prisma generate && next build`, `vercel.json` (`fra1`), `DEPLOYMENT.md` runbook. |
| `520a81e` | Notifications: unified `Notification` model (`targetUserId` null = broadcast to all students, set = one user) + `NotificationRead` (row-per-read, no flag), admin compose at `/dashboard/admin/notifications`, `NotificationBell` on shadcn `Popover`, 60 s poll + focus refetch. `src/lib/notify.ts` + `src/lib/notifications.ts`. |
| `f18bb72` | Code orders require a **payment-proof screenshot** (Neon Object Storage, private `payment-proofs` bucket) + in-app notifications on new-request / approve / reject. |
| `2bd8995` | `CodeOrder` request queue replaces online payment — student submits phone + payment reference → PENDING → admin approve (issues used `PrepaidCode` + `Enrollment` in one txn) / reject with reason. Admin queue at `/dashboard/admin/code-orders`. |
| `d314198` | Remove Stripe entirely — deleted `src/lib/stripe.ts`, the webhook route, the `stripe` package; `/api/checkout` is now free-courses-only. |
| `97ae8ba` | Loosen prepaid-code redeem rate limit (25/user + 60/IP per 10 min), double-submit guard. |
| (earlier) | Prepaid course-access codes: `PrepaidCode` model, admin batch-generate UI, student `RedeemCodeForm`, atomic single-use redeem endpoint (2nd sanctioned enrollment path). |
| (earlier) | UI/UX overhaul Phases 1–3 (student overview rings + gamification; admin charts + stat cards + Quick Actions + sortable student table; lesson workspace slide-over panel + route transitions + mobile pass). |
| (earlier) | Admin→student announcement banner (`SiteSettings.announcement`). |

Plus this session (post-`e6cd486`, **dashboard-only, no code changes**):
deployed to Vercel, fixed `DATABASE_URL` quoting + `NEXTAUTH_URL` domain, created
the free Upstash Redis DB, added all 6 storage/Redis env vars, redeployed,
verified health endpoints.

Session 2026-09-07 (cont., ~13:00–14:40): closed out §4.1 and §4.2 (verification
only), then switched the email provider **SendGrid → Brevo** (`src/lib/email.ts`
now POSTs Brevo's HTTP API — no SDK; `@sendgrid/mail` uninstalled; `BREVO_API_KEY`
+ `EMAIL_FROM` + `EMAIL_FROM_NAME` env vars; `.env.example` + CLAUDE.md +
DEPLOYMENT.md updated). Commit `9a09eee`, deployed to Production. Reason: Brevo's
free tier is 300 emails/day with no credit card (SendGrid is 100/day with
stricter signup).
Then set up the Brevo account live with the owner: created it, verified two
senders, generated an API key, set all three env vars on Vercel (Production),
redeployed, and drove the live `/api/auth/otp/send` end-to-end. Result: the whole
chain works **except** Brevo itself — new accounts need transactional sending
activated by Brevo support ("Your sending platform is currently disabled"). One
early test email did reach Gmail before the throttle kicked in. Owner chose to
wait on a Brevo support ticket rather than switch to the Gmail-SMTP fallback. See
§4.3 for the exact state and how to finish.
§4.1 confirmed already working on prod via live data/storage inspection (approved
`CodeOrder` + real 562 KB screenshot in the private bucket + issued/used
`PrepaidCode` + 3 enrollments + broadcast notifications). §4.2 — `test` branch
schema already in sync; `vitest run` green at 27/27.
Then built the **staging privacy gate** in `src/middleware.ts` (`STAGING_PASSWORD`
env var → site-wide Arabic password page / API 401 / `noindex`; httpOnly cookie
30 d; `/staging-unlock` form). `tsc` + `eslint` + `next build` clean; tested
locally (gate page, API 401, wrong/right password, cookie passthrough,
dashboard-auth preserved). Owner set `STAGING_PASSWORD` on Vercel + redeployed —
**gate is live** (verified: `/` → password page, `/api/*` → 401).

Then a **performance pass** (commits `e74cbee`, `270f18b`, `97066bd`, `1d16351`):
1. Added missing DB indexes — `QuizAttempt(userId)` + `(quizId)` (it had none),
   `Question(quizId)`, `Enrollment(courseId)`, `LessonProgress(lessonId)`.
   **Pushed to the `test` branch; PROD BRANCH STILL NEEDS `npx prisma db push`**
   (run locally against `.env` = prod; `CREATE INDEX` only, non-destructive — the
   automation classifier blocked doing it from here).
2. `getSiteSettings()` → 60 s per-instance TTL cache (`bustSiteSettingsCache()`
   from the settings PATCH). Was an uncached read on every page.
3. `GET /api/notifications` → one query instead of two (unread derived from the
   20 rows); bell poll 60 s → 90 s.
4. Student dashboard: narrow `select` on the enrollments query (was pulling every
   `Lesson.contentHtml`), `quizAttempt.count()` instead of `findMany()` for the
   pass count, `select` on the progress query.
`tsc` + `eslint` + `next build` + `vitest` (27/27) all clean.

Then **new-course-request alerts** (commits `5f6c3ed`, `fba0cfd`): a chime +
desktop notification in `NotificationBell` on any new unread (mute toggle,
`useLocalStorage`), plus **email** (`sendEmail` / `ADMIN_ALERT_EMAILS`) and
**Telegram** (`src/lib/telegram.ts`, `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID`)
sent from `POST /api/code-orders` inside `after()`. Telegram is **LIVE** —
bot `@MH_pings_bot`, env vars set on Vercel, verified. Email waits on Brevo.
`tests/setup.ts` mocks `after()`.

Then **admin tooling**: (a) `19a200d` **module/lesson reordering** — ▲/▼ move
controls, `PATCH /api/{modules,lessons}/[id]` `{action:"move",direction}` swaps
`order` with the adjacent sibling in a txn; (b) `0364e01` **prepaid-code delete**
(unused only, 409 on used) + **CSV export** (`GET /api/prepaid-codes/export`);
(c) `4987862` **user role management** — promote/demote + disable/enable at
`/dashboard/admin/users` (`PATCH /api/users/[id]`), guards: no self-edit, keep
≥1 active ADMIN; disabled users blocked at login. New `User.disabledAt` column.
Tests: `reorder`, `prepaid-code-delete`, `users-manage` (suite 40/40).

Then the **marketing-page redesign — IMPLEMENTED** (commit `578d9cc`, deployed).
Canvas that seeded it (kept for reference):
https://claude.ai/code/artifact/ba1a8116-42f6-47be-9680-02a62794df81
- `src/lib/course-meta.ts` — `courseMeta(modules)` (module/lesson/quiz counts +
  duration) + `formatDuration()`.
- `CourseCard` — grade-colored top bar, lesson/quiz meta row, price / "مجاني"
  badge, optional `featured` flag. New props `price`, `meta`, `featured`.
- Homepage: product-mockup hero, kept the real animated stats band, new "لماذا"
  (4 value props), curriculum preview from the first course's real data, new
  card grid, 3-step "كيف تبدأ", richer instructor block, `<details>` FAQ,
  combined CTA + `#contact`. **Testimonials NOT shipped** (no data — would be
  bracketed placeholders on prod).
- Catalog + course-detail: richer cards; detail has a hero band, `<details>`
  per-module curriculum, and a sticky buy sidebar (price / EnrollButton /
  RedeemCodeForm / "يشمل" list); RequestAccessForm moved full-width below.
- Instructor photo is still a marked placeholder (owner-supplied file needed).
- `/` + `/courses` still prerender (ISR); tsc/eslint/build clean.
- Prod `prisma db push` (perf indexes + `User.disabledAt`) was **done** by the
  owner and `4987862` pushed — the ⚠️ box that used to be here is resolved.

### Autonomous fix session (2026-09-07 evening — owner away, "solve everything solvable")
Screenshot tooling first: `scripts/shots.mjs` + `npm run shots` (Playwright,
added as a devDep) — captures pages at desktop + mobile. Then a full sweep of
every page (logged in as seeded admin + student on the `test` branch).

| Commit | Change |
| --- | --- |
| `ad67059` | **Mobile overflow fix** — wide dashboard tables pushed a page-level horizontal scroll. `min-w-0` on `DashboardShell`'s content column + icon-only `UserRowActions` below `sm`. |
| `29f949a` | **Honest homepage stats** — dropped fabricated "5,000 students / 98%"; now real computed counts (courses/modules/lessons/quizzes), hidden with no content. Vanity fields removed from the admin settings form (schema untouched). |
| `963c1f1` | **"delete quiz" button** in the lesson editor; route hardened (404 + `$transaction`). Test `quiz-delete.test.ts`. |
| `3b1793f` | **Code-order gaps** — student self-cancel of a PENDING request (`POST /api/code-orders/[id]/cancel`, hard delete + best-effort blob cleanup, "إلغاء" button); email to student on approve/reject (`after()`, best-effort, live with Brevo). Test `code-order-cancel.test.ts`. |
| `c1d333a` | **Session hardening** — `session.maxAge` 30 d → 7 d; `requireAdmin()` re-reads the account (disabled/demoted admin locked out next request, one PK lookup). The "keep ≥1 admin" 409 is now defense-in-depth. |
| `e394a51` | **CI** — `.github/workflows/ci.yml`: typecheck + lint + `prisma validate` on every push; build + Vitest gated on a `DATABASE_URL` repo secret (owner to add, pointing at the **test** branch). |
| `f542399` | **Payment-proof cap** — max 15 stored objects/user (~75 MB) on top of the hourly rate limit. Fail-open. |
| `578d9cc`+ hero fixes (`d85afee`) | marketing redesign eyeballed via screenshots — fixed the hero floating instructor card overlap, donut play icon, mobile CTA width; curriculum-preview guard. |

`tsc` + `eslint` + `next build` + `vitest` (**50/50**) clean throughout. No
schema changes — everything deployable as-is.
Test-branch note: it has seed + demo data from the screenshot sweep and ~20
leaked `Test User` fixture rows; harmless (tests are ID-isolated and pass), but
`npx prisma db push --force-reset` against `.env.test` cleans it if wanted.

Next real work: owner tasks — publish the 3 real courses (+ delete the `jj`
test course), Brevo support ticket, real content/videos/images, custom domain,
lawyer review. Code-side the platform is in good shape; see
[[ledger-academy-known-problems]] for what's left and why each needs the owner.

---

## 6. How to resume in a new session

1. Read this file, then `PROJECT_STATE.md` §3–4 and `CLAUDE.md`.
2. `cd "c:/Users/hp/Desktop/Mansa 1/accounting-elearning"` · `git status` (clean,
   `main = origin/main` as of the autonomous fix session).
3. Pick up wherever the owner directs. Code-side is in good shape; the open work
   is owner-side (publish real courses, Brevo ticket, content, domain, lawyer).
4. Schema changes still go through `npx prisma db push` (no migration history) and
   must be pushed to **both** the prod and `test` Neon branches; Vercel does not
   migrate.
5. "Commit as you go" — commit each completed working change without waiting to be
   asked (project rule).
