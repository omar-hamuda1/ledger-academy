# Project State — Ledger Academy

Last updated: 2026-09-07 (Stripe removed → code-based access; payment-proof upload; admin→student broadcast notifications. See section 3.)

> **Deployed to Vercel as private staging: https://ledger-academy-liart.vercel.app** (commit `e6cd486`, region `fra1`, prod Neon branch). All storage + Upstash env vars now set and **verified end-to-end on prod** (payment-proof upload → approve → code+enrollment). The `test` branch schema is in sync and `vitest run` is green (27/27). See **`HANDOFF-2026-09-07.md`** for deployment state, credentials, env-var status, and the prioritized next-steps list — next real work is §4.3 (Brevo email keys, owner action) + §4.4/§4.5.

## 1. Architecture & Tech Stack

- **Framework**: Next.js 16 (App Router) + TypeScript, React 19
- **Styling**: Tailwind CSS v4 (CSS-based config, no `tailwind.config.ts`), Cairo font for Arabic
- **Database**: **Postgres on Neon** (project `late-tree-77912979`). `DATABASE_URL`/`DATABASE_URL_UNPOOLED`/`NEON_BRANCH` live in `.env` (Neon-managed); app secrets live in `.env.local`. No migration history — schema changes go through `prisma db push`.
- **ORM**: Prisma v6.19.3 (classic CLI — do not upgrade to the v8 "Prisma Developer Platform" line, it has a completely different, incompatible CLI with no `migrate`/`generate` commands)
- **Auth**: NextAuth v4 (`next-auth` + `@next-auth/prisma-adapter`), credentials provider, JWT sessions, role (`ADMIN`/`STUDENT`) baked into the session
- **Payments**: none — no online payment gateway. Stripe was removed 2026-09-07 (no Egypt merchant support). Paid courses are unlocked by a prepaid code or an admin-approved `CodeOrder`; money moves manually (Vodafone Cash / InstaPay / bank / cash). `price` is a display figure. Currency **EGP** (`formatEgp`).
- **Icons**: `lucide-react` (v1.x — note: this version dropped brand/social icons like Facebook/Instagram/YouTube; hand-rolled SVGs live in `src/components/ledger-academy/SocialIcons.tsx`)
- **Validation**: Zod on every API route
- Runs from `Mansa 1/accounting-elearning/` (the parent folder name has a space, which is why the app itself lives in a subfolder — `npx create-next-app` rejects a directory named "Mansa 1" directly)

## 2. Platform Purpose

**Ledger Academy** — an Arabic (RTL), dark-navy-and-gold themed e-learning platform teaching **Business Administration (إدارة الأعمال)** to Egyptian secondary-school students (3 grade levels: الصف الأول/الثاني/الثالث الثانوي). Content is presented by "محمد حسين," who holds the platform's `ADMIN` role.

**Hard rule**: the word "مدرس" (teacher) must never appear anywhere in the UI. Use **"محاضر"** (lecturer) or "مدرب" instead. This has been enforced everywhere (dashboard titles, marketing copy, etc.) and verified via full-codebase grep.

## 3. Completed

- **Marketing site**: homepage, `/courses`, `/courses/[slug]`, `/pricing` — all Arabic RTL, dark/gold theme, pulling real data from the DB (no hardcoded content)
- **Auth**: register, login (role-based redirect: `ADMIN` → `/dashboard/admin`, else → `/dashboard/student`), NextAuth session with role/id
- **Student dashboard** (`/dashboard/student`): enrolled courses with real progress bars, lesson player with video + mark-complete + auto-advance to next lesson, quizzes (list + take + auto-graded results), interactive tools page (break-even calculator, SWOT board)
- **Admin dashboard** (`/dashboard/admin`): overview stats, lesson management (create/edit/delete lessons, attach downloadable resources, build quizzes with multiple-choice questions), student progress table, user list, **course pricing control** (`/dashboard/admin/courses` — edit price + publish toggle per course)
- **Enrollment/payment flow**: free courses auto-enroll; paid courses go through real Stripe Checkout (test mode) → webhook (`/api/webhooks/stripe`) creates the enrollment on `checkout.session.completed`
- **Quiz system**: `Quiz`/`Question`/`QuizAttempt` models fully wired — admin authors questions with a marked correct answer, students take quizzes, scoring is computed **server-side only** (client never receives `correctId` before grading)
- **Security pass**: rate limiting (`src/lib/rate-limit.ts` — Upstash Redis when configured, in-memory fallback otherwise, see below) on `/api/register` (5/10min per IP) and login (5/5min per email); security headers (`X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy`) via `next.config.ts`
- **Stripe**: real test-mode integration, verified end-to-end with an actual Stripe CLI–forwarded webhook and a completed test-card payment. Stripe CLI is installed on this machine and authorized against the user's sandbox account.
- **Create-course UI** (`/dashboard/admin/courses/new`): admin can create a brand-new course (title/slug/description/grade/price/publish) via `CreateCourseForm.tsx` → `POST /api/courses`. The slug field must be typed in English/Latin characters by the admin — auto-deriving a URL slug from an Arabic title doesn't work (see bug note below) so it's manual by design, with live client-side cleanup (`slugify()`) as they type. Both client and server reject a slug with no real alphanumeric characters, and the server checks for slug collisions with a clear Arabic error.
- **Postgres migration**: moved off SQLite to Neon (see Architecture above). Schema pushed and reseeded; 3 extra users, an extra lesson, and a quiz created by hand while on SQLite were NOT migrated (still sitting in the old `prisma/dev.db` file, untouched).
- **Personalized header / session display**: `LedgerHeader.tsx` never called `useSession()` — it always showed a static "تسجيل الدخول" button even when logged in. Now shows a personalized greeting + dashboard link + logout button when authenticated (desktop and mobile), a loading skeleton while session resolves, and falls back to the login button when logged out.
- **Email OTP authentication**: signup and forgot-password both require a verified 6-digit one-time code emailed to the user. Originally built as SMS-to-phone (Twilio), but pivoted to email after confirming with the user that no SMS provider (Twilio, Firebase Phone Auth) offers a real free tier — Egypt is excluded from trial programs and it's pay-per-message indefinitely — whereas email (Resend) has a genuinely free tier. `User.phone`/`phoneVerifiedAt` were added then removed; there is no phone field in the schema. New `OtpCode` model (bcrypt-hashed codes, 5-min expiry, attempt limits, purpose `SIGNUP`/`RESET`, keyed by `email`), `src/lib/email.ts` (Resend, with a console-log dev-mode fallback when `RESEND_API_KEY`/`EMAIL_FROM` aren't set). New routes: `POST /api/auth/otp/send`, `POST /api/auth/otp/verify`, `POST /api/auth/reset-password`; `/api/register` now requires a verified email OTP. New page `/forgot-password`. Verified end-to-end (send → verify → register, and send → verify → reset → login with new password, replay-protection on reuse) — Resend is now connected and confirmed sending real emails (to the account owner's address only, see Pending).
- **Design system overhaul**: `globals.css` now has a real `@theme` block (`--color-navy-*`/`--color-gold-*`, `--radius-card`/`--radius-control`, `--shadow-card`/`--shadow-elevated`/`--shadow-glow-gold`, `--animate-fade-in`/`-scale-in`/`-slide-up`/`-pop`) instead of the untouched Next.js starter file. Root `<html>` now sets `lang="ar" dir="rtl"` once and loads Cairo globally (previously only marketing/auth pages loaded Cairo — every dashboard page was silently falling back to a Latin font after login). Fixed stale metadata (was "Accounting E-Learning" in English). Deleted the legacy `/dashboard/instructor` route entirely (unstyled light-mode leftover, unused since محمد حسين is `ADMIN` not `INSTRUCTOR`) and its middleware reference. Extracted the duplicated course-card JSX (was copy-pasted verbatim in two files) into `src/components/ledger-academy/CourseCard.tsx`, which also varies its icon by grade instead of the same icon for every course. Applied shadow/radius tokens and hover-lift transitions across admin cards, auth forms, and the student dashboard; restyled the previously-bare `AdminUsersPage` table (avatars, role badges, empty state, zebra hover).
- **Admin control expansion**: course editing is no longer price/publish-only — `/dashboard/admin/courses/[id]/edit` (`EditCourseDetailsForm.tsx`) now covers title, description, grade, and thumbnail URL too (`updateCourseSchema` extended accordingly). Added full module (unit) management, which didn't exist before: `POST /api/modules`, `DELETE /api/modules/[id]`, `AddModuleForm`/`DeleteModuleButton` wired into `/dashboard/admin/lessons` — previously modules could only be created via `prisma/seed.ts`. Added missing `confirm()` dialogs to resource/question delete buttons (only lesson delete had one before). New `SiteSettings` singleton model + `/dashboard/admin/settings` page: admin now controls the homepage's student-count/satisfaction-rate stats, contact email/phone (previously hardcoded `info@ledgeracademy.com` / placeholder phone), and can toggle the break-even calculator / SWOT board on or off for students — all previously hardcoded/unconditional.
- **Student interactivity pass**: added `sonner` (toast notifications, mounted globally in `Providers`) and `canvas-confetti` (confetti burst on quiz pass, score ≥50%). Lesson mark-complete now shows a toast + pop animation instead of a silent instant swap. Quiz results now animate in (`animate-scale-in` on the score card, staggered `animate-slide-up` per question) with confetti + toast on pass/fail. Student home page gained a small stats strip (lessons completed all-time/this-week, quizzes passed — computed from existing `LessonProgress`/`QuizAttempt` data, no schema change) and hover-lift on course cards. SWOT board's previously-silent `localStorage` autosave now shows a brief "تم الحفظ ✓" fade-in confirmation. Added `loading.tsx` skeleton screens for the student home and quizzes list (previously no loading states existed anywhere in the app besides one auth spinner).

- **Pre-scale performance pass**: converted the three fully-public marketing pages (home, course catalog, pricing) from `force-dynamic` to ISR (`revalidate = 300`) to cut database load — deliberately left the course-detail page as `force-dynamic` since it renders a per-user enrollment state server-side. Added pagination (`src/components/admin/Pagination.tsx`, `PAGE_SIZE = 20`) to the three unbounded admin lists (users, progress, courses). Switched course thumbnails to `next/image` (verified it actually optimizes/resizes remote images, not just passes them through) — required allowing any HTTPS host in `next.config.ts` since thumbnails are arbitrary admin-supplied URLs.

- **Full security/correctness/quality audit (2026-09-06)**: ran a 3-way parallel audit (security/authorization, correctness/data-integrity, code quality) covering every API route and the Prisma schema, verified every finding by reading the actual code myself, then fixed everything real-world-exploitable:
  - **Fixed — payment bypass**: `POST /api/enrollments` let any logged-in user enroll in any paid course with zero price check, completely bypassing Stripe. It was unused by any UI (checkout already handles enrollment correctly), so it was deleted outright rather than patched.
  - **Fixed — quiz answer-key leak / paywall bypass**: `POST /api/quizzes/[id]/attempts` never checked the caller was enrolled, so any user could take any quiz (including unpurchased courses') and the response handed back `correctId` for every question. Now checks enrollment first.
  - **Fixed — progress IDOR**: `POST /api/progress` accepted any `lessonId` with no ownership check, letting a user fabricate completion/watch-time for lessons in courses they never bought. Now checks enrollment first.
  - **Fixed — cross-course content leak**: the lesson-detail page verified enrollment in the course matched by the URL's `slug`, but then fetched the lesson purely by `lessonId` with no check it actually belonged to that course — so a student enrolled in one (even free) course could view any paid course's lesson content by URL-guessing/observing a `lessonId`. Now the lesson query is scoped to the resolved course.
  - **Fixed — delete-lesson/module crash**: `LessonProgress`/`Quiz` foreign keys to `Lesson` are `ON DELETE RESTRICT`; deleting a lesson or module that any student had touched (progress row) or that had a quiz attached threw an uncaught 500 with the admin UI just silently doing nothing. Both delete routes now clean up `QuizAttempt`→`Quiz` and `LessonProgress` in a transaction first (mirrors the pattern the quiz-delete route already used correctly).
  - **Fixed — OTP resend confusion**: requesting a second OTP code and then entering the first email's code failed (verify only ever checked the newest row) and burned an attempt on the wrong row. `otp/send` now deletes prior unverified codes for that email+purpose before issuing a new one.
  - **Fixed — Stripe double-charge risk**: checkout session creation had no idempotency key, so a rapid double-click could create two separately-payable Stripe sessions. Added a per-user/per-course/per-minute idempotency key.
  - **Fixed — lesson/module order collisions**: new lesson/module `order` was computed as `array.length + 1`, which re-mints a colliding value after a middle item is deleted. Now uses a `_max` aggregate instead.
  - **Fixed — inconsistent error language**: ~15 API route files had English guard-clause errors ("Forbidden", "Unauthorized", "Invalid input.", etc.) shown directly to Arabic-speaking users; all translated.
  - All of the above verified with real exploit attempts against the running app (not just read-through) — e.g. actually tried the cross-course lesson access and confirmed it now 404s instead of leaking content, actually deleted a module with real progress/quiz-attempt rows attached and confirmed no crash and no orphaned rows.
  - **Resolved with user**: the two junk test courses were confirmed as accidental real data entry and deleted (with full cascade cleanup, verified no orphaned rows). The stale dated snapshot (`PROJECT_STATE_LedgerAcademy_2026-09-05_15-42.md`) was corrected in place to match current reality rather than deleted, per the user's preference to keep the dated record.
- **Removed the unused `INSTRUCTOR` role (2026-09-06)**: the `/dashboard/instructor` route was already deleted in an earlier cleanup pass, and the middleware only ever distinguished `ADMIN` vs. "everyone else" — so `INSTRUCTOR` was dead weight in the `Role` enum, `next-auth.d.ts` types, and the admin users-page role-label/badge maps, with zero users actually holding it (confirmed via a direct DB query before touching anything). User decided to formally merge it away rather than build a real instructor experience. `Role` is now just `ADMIN | STUDENT`; schema pushed with `--accept-data-loss` (safe — confirmed no rows referenced the removed value first).
- **Old SQLite cleanup (2026-09-06)**: inspected `prisma/dev.db` directly with Node's built-in `node:sqlite` before touching anything — confirmed it held only 2 placeholder test users, one junk "koko" lesson, one empty quiz, and zero enrollments (courses/lessons were already duplicated in Postgres via the seed script). Nothing of value to migrate, so the user approved deleting both `prisma/dev.db` and the stale SQLite-dialect `prisma/migrations/` folder (unused since the project moved to `prisma db push`, and a latent risk if `prisma migrate` were ever run by accident against Postgres). `.gitignore`'s now-dangling `dev.db` entries were removed too.
  - **Larger optional cleanup identified but not done (needs a scope decision)**: the register/forgot-password OTP flow pages share a lot of near-duplicate code (candidate for extraction, same pattern as `CourseCard.tsx`); admin form input styling is copy-pasted across ~7 components instead of a shared `Input` component; `LedgerHeader.tsx` (the single most-reused component) and `EnrollButton.tsx` and `CreateCourseForm.tsx` are still on the pre-design-system tokens (`amber-400`/`slate-950`/`rounded-2xl`) while `EditCourseDetailsForm.tsx` (same conceptual form) is fully migrated — causes visible style mismatch between "New Course" and "Edit Course" admin screens.

- **Git version control + automated tests (2026-09-06)**: this project had no git repository and zero automated tests until a devil's-advocate review surfaced both as real risk. Fixed: `git init`, pushed to a private GitHub repo (`github.com/omar-hamuda1/ledger-academy`); added Vitest running against a dedicated Neon `test` branch (copy-on-write off production, never touches real data) with regression tests for all 5 of the 2026-09-06 security-audit fixes except the order-collision fix and the Stripe idempotency key (not yet covered). See CLAUDE.md's "Testing" section for how to run/extend these and the fixture-cleanup gotcha that was found and fixed while building them (two test files bypassed the shared cleanup helper and leaked fixtures onto the test branch across runs — fixed and verified with repeated runs showing zero leftovers).
- **Stripe/Egypt payment research (2026-09-06)**: see the "Pending / Known Gaps" Stripe entry below — Stripe doesn't support Egypt-based merchants; researched Paymob/Fawry as native alternatives; blocked on the user completing a sole-proprietorship business registration before either can be integrated.
- **Admin lockout recovery + second admin (2026-09-06)**: this project only ever had one ADMIN account, with password reset for it depending on the same email system that isn't always working — a real, previously-unrecoverable lockout risk. Fixed two ways: added `npm run admin:recover` (`scripts/admin-recovery.ts`, see CLAUDE.md's "Authentication" section), a break-glass script that sets a password and forces `role: ADMIN` directly in the database, bypassing email entirely; and promoted the user's own personal account (`omarhamuda20@gmail.com`) from STUDENT to a second standing ADMIN (role change only, password untouched). There are now two working admin logins.
- **Privacy Policy + Terms of Service pages (2026-09-06)**: added real Arabic `/privacy` and `/terms` pages tailored to this platform (data collected, minors/parental-awareness language, third parties, no ad-tracking, user rights, content-piracy prohibition, Egyptian governing law), both carrying a visible disclaimer that they're a general template needing real lawyer review given minors' data is involved. Linked in `LedgerFooter.tsx` and as a consent line on the register form.
- **Secret rotation + backup verification (2026-09-06)**: `.env.local`'s contents had been printed in plaintext earlier in a session, so `NEXTAUTH_SECRET` was rotated (invalidates old sessions, harmless). User declined rotating the Stripe test keys for now (low risk, test-mode only). Separately, checked Neon's actual backup situation via `neon projects list` — the project is on the **free plan** with only **6 hours** of point-in-time-recovery history (`history_retention_seconds: 21600`); upgrading for longer retention is an unmade cost decision. Production error monitoring (e.g. Sentry) remains undone, deliberately deferred until real deployment exists to monitor.
- **Admin audit log (2026-09-06)**: new `AuditLog` model + `src/lib/audit.ts` (`logAudit()`, best-effort — a logging failure never blocks the real admin action). Wired into all 13 `requireAdmin()`-gated mutation routes (courses, modules, lessons, quizzes, questions, resources, settings — create/update/delete). New `/dashboard/admin/audit` page (paginated, same pattern as `users`/`progress`/`courses`) shows who did what and when. Regression-tested (`tests/api/audit-log.test.ts`) — while building it, found and fixed a real bug in two pre-existing test files that bypassed the shared cleanup helper and were about to start leaking `AuditLog` rows too; `cleanupUser()` now clears a user's audit rows as well.
- **Rate limiting: Upstash Redis backend added (2026-09-06)**: discovered the in-memory limiter wasn't just a "won't scale later" problem — it likely wouldn't work correctly from day one on a serverless deployment (e.g. Vercel), since each request can hit a different instance with its own empty memory. `checkRateLimit()` is now `async` and uses Upstash Redis (genuinely free tier, no credit card, verified) when `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` are set, falling back to the in-memory implementation otherwise. Also fixed a real unbounded-memory-growth bug in the in-memory path (it never evicted expired entries) with a periodic sweep. The Redis path is code-complete and the fallback is tested, but the Redis path itself hasn't been exercised against a real Upstash instance yet — no credentials exist.
- **Accessibility pass (2026-09-06, static code review — no browser/device testing tool available this session)**: confirmed alt text, aria-labels, and keyboard-accessible interactive elements are all already in good shape. Calculated real WCAG contrast ratios rather than guessing, found `text-slate-500` on the navy-950 background at 4.24:1 (fails AA's 4.5:1 minimum for normal text), fixed by bumping to `text-slate-400` (7.87:1) across 33 real-content occurrences in 21 files — left `placeholder:text-slate-500` alone since placeholder contrast is a weaker requirement. Real device/viewport/screen-reader testing still hasn't been done.

- **UI/UX overhaul — Phase 1: Student Overview (2026-09-06)**: see section 5 below for the full spec and status — this is a large, explicitly multi-phase request, paused after phase 1 for the user's review. Set up shadcn/ui (remapped its default theme onto navy/gold, since it doesn't match out of the box — see CLAUDE.md's "Design system" section for the exact gotcha), added `framer-motion` and `recharts`. Redesigned `/dashboard/student` (`page.tsx`): circular progress rings (`CircularProgress.tsx`) replace the old linear bar, a real (not fabricated) gamification widget (`GamificationWidget.tsx`) shows a day-streak computed from actual `LessonProgress` timestamps plus 7 achievement badges keyed off real completed-lesson/quiz counts, and the course list (`StudentCourseList.tsx`) now has a staggered entrance animation and a spring-based hover lift. Verified against the live app with a real temporary test student (real enrollment/progress/quiz data, cleaned up after) — confirmed exact expected values (50% ring, 7 badges, streak text) in the actual rendered HTML, not just a type-check.

- **UI/UX overhaul — Phase 2: Admin dashboard (2026-09-07)**: rebuilt `/dashboard/admin` (`page.tsx`) from two bare count cards into a real overview. New `src/lib/admin-metrics.ts` (`getAdminMetrics()`) computes every number from existing rows — no schema change, no fabricated data: 6 stat cards (students, enrollments, courses/published, completed lessons, quiz pass count, pass rate + avg score) via `AdminStatCards.tsx` (framer-motion stagger + the existing `AnimatedCounter`), an 8-week activity area chart (new enrollments vs. lesson completions, bucketed by week) and a horizontal "enrollments per course" bar chart via `AdminCharts.tsx` (Recharts, themed to navy/gold, `dir="ltr"` on the chart box only). New `QuickActions.tsx` — a hand-rolled accessible dropdown (click-outside + Esc, `AnimatePresence`; deliberately not shadcn's `dropdown-menu` to avoid re-running `shadcn add` and risking the globals.css theme-remap, per CLAUDE.md) linking to new-course / lessons / settings / audit. The 4 nav cards are kept below.
  - **Advanced student table**: `/dashboard/admin/progress` (`page.tsx`) now passes rows to `StudentProgressTable.tsx` (client) with search (name/email), click-to-sort on every column (courses, completed lessons, quizzes passed, last activity, join date), and client-side pagination (20/page). Dropped server-side pagination there since sort/filter must span all rows; page query caps at `take: 500` (`MAX_ROWS`) with a visible "capped" note — move search/sort server-side if that's ever hit. Last-activity = max of the student's `LessonProgress.updatedAt` / `QuizAttempt.takenAt`.
  - **Verification**: `tsc --noEmit` clean, `next build` clean (compiles the RSC/client boundary + the Recharts client bundle), all new Prisma queries (incl. `orderBy: { enrollments: { _count: "desc" } }` and the `quizAttempt` aggregate) run against the real DB, and the unauthenticated route still correctly 307s to login (no SSR crash). **Not browser-verified** — no browser tool this session and no admin credentials on hand, so the visual result (chart rendering, dropdown animation) hasn't been eyeballed the way Phase 1 was. Production DB currently has 3 courses and zero enrollments/progress, so every chart/table hits its empty-state path today; those empty states are explicitly coded.
  - **Not done in Phase 2** (need small backend work, out of scope for a UI pass): the table's "bulk actions" (row-select + role promote/demote/disable — overlaps the still-open "No user role management UI" gap and needs its own security review since it grants ADMIN). *(The spec's "تنبيه للطلاب" student-broadcast was done separately — see the announcement-banner entry below.)*

- **UI/UX overhaul — Phase 3: lesson workspace + transitions + mobile (2026-09-07)**: three pieces.
  - **Lesson workspace panel** (`src/components/course/LessonWorkspacePanel.tsx`, client): a left-anchored slide-over on the lesson-player page, opened by a fixed "الأدوات والملفات" pill button, holding the lesson's attached files **and** — gated by the existing `SiteSettings.showBreakEvenTool` / `showSwotTool` flags — the `BreakEvenCalculator` and `SwotBoard`, so a student uses them without leaving the video. Tabbed when both files and tools are present, single view otherwise, renders nothing (no button) when there's neither. framer-motion slide (opacity-only under `prefers-reduced-motion`), scrim + Esc close, active tab remembered in `localStorage`. The old inline `ResourceList.tsx` is **deleted** — the panel fully replaces it; `contentHtml` still renders inline on the page.
  - **Route transitions** (`src/app/dashboard/template.tsx`, client): a `template.tsx` re-mounts per navigation, so it carries a short (~0.18s) cross-page fade for every `/dashboard/*` route. **Opacity-only on purpose** — animating `transform`/`filter` on an ancestor establishes a containing block for `position: fixed` descendants (the panel trigger, `DashboardShell`'s mobile overlay) and would make them jump for the animation's duration. Honors `prefers-reduced-motion`.
  - **Mobile pass**: `LessonSidebar` (fixed `w-72` course nav) is now `hidden lg:block`; a no-JS `<details>` "دروس الكورس" (`MobileLessonNav`, same file) carries the same lesson tree above the content on `<lg`. Lesson content padding drops to `p-4 pb-24` on mobile (clears the floating panel button). `QuickActions` dropdown re-anchored `right-0` (was `left-0`) so it opens into the viewport in RTL instead of off-screen on a phone; `AdminCharts` thins the activity chart's x-axis ticks and trims the popularity chart's y-axis so 8 Arabic week-labels / long course titles don't collide narrow.
  - **Verification**: `tsc` + `next build` clean. **Not browser-verified** (no browser tool / admin creds this session) — the slide animation, the `<details>` marker suppression across browsers, and real mobile-viewport behavior haven't been eyeballed.

- **Admin → student announcement banner (2026-09-07)**: the smallest real step toward a notification system (the "تنبيه للطلاب" idea from the UI-overhaul spec). `SiteSettings` gained `announcement String?` + `announcementActive Boolean @default(false)` (extends the existing singleton, not a new model — `prisma db push` run by the user, two additive columns). `updateSiteSettingsSchema` accepts both (announcement trimmed, ≤500 chars, empty → `null`). `/dashboard/admin/settings` has a new "إعلان للطلاب" section (textarea + 500-char counter + activate checkbox), saved through the existing audit-logged settings route. `src/components/dashboard/AnnouncementBanner.tsx` (client) shows a dismissible gold megaphone bar on `/dashboard/student` when active + non-empty; dismissal is per-viewer in `localStorage` keyed to a hash of the text, so editing the message re-shows it for everyone while an unchanged one stays hidden once dismissed. A per-user notification *feed* (new `Notification` model, bell + unread count, polling) is the documented next tier if a targeted/event use case appears — see the notification-system discussion.

- **Prepaid course-access codes (2026-09-07)**: a course-level redemption system so the lecturer can sell/hand out single-use codes offline that unlock a whole course, bypassing Stripe. New `PrepaidCode` model (`code` unique, `courseId`, `isUsed`, `usedById?`, `usedAt?`, `createdAt`; `onDelete: Cascade` from Course) — **requires `prisma db push` on both the prod and `test` Neon branches**. Codes are 12 chars from a 31-symbol unambiguous alphabet (no I/O/0/1), stored bare, shown/redeemed dash-grouped; `src/lib/prepaid-codes.ts` has `generateCode`/`generateCodeBatch`/`normalizeCode`/`formatCode`.
  - **Admin** (`/dashboard/admin/prepaid-codes`, in the sidebar + overview cards + Quick Actions): `GeneratePrepaidCodesForm` (pick course + quantity ≤200 → `POST /api/prepaid-codes`, shows the fresh batch with copy-all), `PrepaidCodesFilters` (course + used/unused, via URL params), `PrepaidCodesTable` (code + course + status + who redeemed + dates, per-row copy). Generation is audit-logged (`prepaid_codes.generate`). `Pagination` gained an optional `query` prop to carry filters across pages.
  - **Student**: `RedeemCodeForm` ("أدخل كود الكورس") on the course-detail page (passes that `courseId`, shown only to logged-in non-enrolled users) and on the student dashboard home (no `courseId` — unlocks whatever course the code belongs to, then routes there).
  - **`POST /api/prepaid-codes/redeem`** is the **second sanctioned enrollment-granting path** (see CLAUDE.md's updated invariant). Session-required, rate-limited per user + per IP. Atomic single-use claim (`updateMany where isUsed:false` + `enrollment.create` in one `$transaction`; racing redeem → 409, no double grant), `P2002` rolls back so a code isn't burned if the student got enrolled elsewhere mid-flight, explicit already-enrolled pre-check (doesn't consume the code), optional `courseId` cross-check. Regression suite: `tests/api/prepaid-code-redeem.test.ts` (6 cases). `tests/helpers/fixtures.ts` gained `createPrepaidCode` and `cleanupCourse` now clears codes.
  - **Verified 2026-09-07**: 6/6 redeem tests pass against real Postgres; schema (columns, `code` unique, both indexes) confirmed on both branches via SQL; routes return correct auth codes; no SSR crashes.

- **Stripe removed + code-purchase request queue (2026-09-07)**: the user decided to drop online payment entirely (no usable Egypt gateway) and sell access as codes. **(a) Stripe removal**: deleted `src/lib/stripe.ts`, `src/app/api/webhooks/stripe/route.ts`, uninstalled the `stripe` package, dropped `STRIPE_*` from `.env.example`. `/api/checkout` is now free-courses-only (`price<=0` → `enrollment.upsert`; paid → 403). `EnrollButton` paid+not-enrolled shows a "activate/request a code below" notice. `MIN_EGP_PRICE` kept as a plain floor. **(b) `CodeOrder` model** (`status` PENDING/APPROVED/REJECTED, `studentPhone`, `paymentNote`, `reviewedBy`, `rejectionReason`, `prepaidCodeId?` unique) + `SiteSettings.paymentInstructions` — **both `prisma db push`ed to prod + `test`**. `CodeOrderStatus` enum. `cleanupCourse`/`cleanupUser` fixtures updated; new `createCodeOrder` helper.
  - **Student** (paid course, not enrolled): two side-by-side cards — `RedeemCodeForm` (have a code) and new `RequestAccessForm` (phone + payment-reference note → `POST /api/code-orders` → a PENDING order; shows `paymentInstructions` above the form; collapses to a "قيد المراجعة" state once submitted). A "طلبات الأكواد" section on `/dashboard/student` lists each order's status + rejection reason.
  - **Admin** `/dashboard/admin/code-orders` (sidebar, overview card with live pending count via `getAdminMetrics().pendingCodeOrders`, Quick Actions): queue filtered to PENDING by default, status chips with counts. `CodeOrdersTable` — **موافقة** (`PATCH /api/code-orders/[id]` `action:"approve"`) issues a used `PrepaidCode` for the student **and** creates the `Enrollment` in one `$transaction`, audit-logged (`code_order.approve`); **رفض** with an inline reason box (`code_order.reject`). A non-PENDING order returns 409. `P2002` / already-enrolled path closes the order without issuing a code.
  - **`/api/code-orders` create**: session-required, rate-limited per user (10/hr) + IP, rejects free courses (400) and already-enrolled (409), dedupes a second PENDING request for the same course.
  - **Verification 2026-09-07**: `tsc` + `eslint` + `next build` clean; tests pass; routes return correct auth codes on the live dev server. CLAUDE.md's enrollment invariant + Pricing & payments section rewritten.
  - **Screenshot upload + notifications (2026-09-07, same day)**: the request now **requires a payment-proof screenshot** and both sides get in-app notifications.
    - **Storage**: Neon Object Storage (S3-compatible, enabled on the project, `eu-central-1`). Private bucket `payment-proofs` declared in `neon.ts` (`preview.buckets`). `src/lib/storage.ts` → `paymentProofStore` (a `files-sdk` `Files` instance or `null` when `AWS_*` env vars absent — nullable-client pattern). **Needs `neon deploy` to provision + `neon env pull` for the S3 creds.** Packages added: `files-sdk`, `@aws-sdk/client-s3`, `@aws-sdk/s3-presigned-post`, `@aws-sdk/s3-request-presigner`.
    - **Flow**: `RequestAccessForm` uploads the image → `POST /api/uploads/payment-proof` (image only, ≤5MB, rate-limited) returns `proofs/<userId>/<uuid>.<ext>` → `POST /api/code-orders` with `paymentProofKey` (rejects any key not under the caller's own prefix). `CodeOrder.paymentProofKey String?` added. Admin table shows each screenshot as a thumbnail via a 1h presigned GET URL generated server-side (plain `<img>`, not `next/image`).
    - **Notifications (v1, superseded same day — see next entry)**: introduced a per-user `Notification` model + `NotificationBell`. Reworked hours later into the broadcast model below.
    - **Verification 2026-09-07**: `tsc` + `eslint` + `next build` clean.

- **Notification system — broadcast + `NotificationRead` (2026-09-07)**: the admin can now compose a message to all students; the per-user model from earlier the same day was unified into one that handles both.
  - **Schema** (needs `prisma db push --accept-data-loss` on **both branches** — the old `Notification.userId`/`readAt` change; ~3 throwaway test rows are dropped): `Notification { id, title, body, href?, targetUserId?, createdById?, createdAt }` — `targetUserId` **set** = one recipient (system events), **null** = broadcast to every `STUDENT`, one row regardless of student count. New `NotificationRead { notificationId, userId, readAt }` (composite PK, `onDelete: Cascade`) is the only read-state store — no flag on `Notification`.
  - **`src/lib/notify.ts`**: `notify({ userId | userId[], ... })` (targeted, best-effort) unchanged for callers; new `broadcastToStudents({ title, body, createdById })`. `src/lib/notifications.ts` `visibleNotificationsWhere(userId, role)` — shared "what can this user see" filter (own targeted + broadcasts if student).
  - **API**: `POST /api/notifications` (admin) composes a broadcast, audit-logged `notification.broadcast`. `GET /api/notifications` returns the caller's visible notifications (newest 20) each with a per-user `read` flag + `unreadCount`. `POST /api/notifications/read` (`{id}` / `{all:true}`) creates `NotificationRead` rows only for notifications the caller can see.
  - **Admin UI**: `/dashboard/admin/notifications` (sidebar "إدارة الإشعارات", overview card, Quick Actions) — `ComposeNotificationForm` (title + message) + a list of sent broadcasts with an "X / Y قرأوه" read count.
  - **Student UI**: `NotificationBell` reworked onto shadcn `Popover` (`npx shadcn add popover` — checked, did **not** touch globals.css) — bell + unread badge in the dashboard header, RTL dropdown, per-item + mark-all read, 60s poll + focus refetch.
  - Not real-time push (SSE/WebSocket) — deliberate, serverless target with no deployment yet; 60s polling + focus refetch is the near-real-time stand-in.
  - **Verification 2026-09-07**: `tsc` + `eslint` + `next build` clean. Tests written (`tests/api/notifications.test.ts`, 3 cases) but **unrun pending `prisma db push` on both branches**; `neon deploy` + `neon env pull` (for the payment-proof bucket from the previous feature) also still pending.
  - **Closed 2026-09-07 (~13:00)**: `test`-branch schema confirmed already in sync (`prisma db push` → "already in sync"); full `vitest run` green (10 files / 27 tests), `notifications.test.ts` ran for the first time and all 3 pass. The `payment-proofs` bucket + `AWS_*` vars are live on prod — verified end-to-end by inspecting prod data: an `APPROVED` `CodeOrder` with its 562 KB screenshot physically in the private bucket, an issued/used `PrepaidCode`, and the enrollment created. See `HANDOFF-2026-09-07.md` §4.1–4.2.

- **Marketing-page redesign (2026-09-07)**: `(marketing)/page.tsx`, `/courses`, `/courses/[slug]`, and `CourseCard` rebuilt from a design canvas (https://claude.ai/code/artifact/ba1a8116-42f6-47be-9680-02a62794df81), staying on the existing navy/gold tokens + components. New `src/lib/course-meta.ts` (`courseMeta(modules)` → module/lesson/quiz counts + duration). Homepage gained: product-mockup hero + trust chips, a "لماذا ليدجر أكاديمي؟" value-prop grid, a curriculum preview built from the first course's real modules/lessons, a 3-step "كيف تبدأ", a richer instructor block, and a no-JS `<details>` FAQ. `CourseCard` now shows a grade-colored top bar, lesson/quiz counts, price (or a "مجاني" badge), and an optional `featured` ("الأكثر طلبًا") flag. Course-detail page: hero band with breadcrumb + meta chips, a collapsible per-module curriculum, and a sticky purchase sidebar (price / `EnrollButton` / `RedeemCodeForm` / "هذا الكورس يشمل" list); `RequestAccessForm` moved to a full-width block below. **Testimonials from the canvas were deliberately not shipped** (no real data — bracketed placeholders don't belong on prod); the instructor photo stays a marked placeholder pending an owner-supplied file. `tsc` + `eslint` + `next build` clean; `/` and `/courses` still prerender (ISR). Commit `578d9cc`. **Not browser-verified** — no browser tool this session and the staging gate is active.
- **New-course-request alerts (2026-09-07)**: when a student submits a `CodeOrder`, admins already got an in-app `notify()`; added three more channels. **(1) Sound** — `NotificationBell` synthesises a two-note chime (Web Audio, no asset) when a poll turns up a genuinely new unread notification (first poll only seeds the seen-id set); mute toggle in the panel header, persisted via `useLocalStorage("ledger:notif-sound")`. **(2) Desktop notification** — a real OS popup when browser permission is granted; a one-time "تفعيل تنبيهات المتصفح" row appears in the panel when permission is `default`. **(3) Email + (4) Telegram** — `POST /api/code-orders` composes one Arabic alert (course, student, phone, payment note, review-queue link) and sends it via `sendEmail()` (`src/lib/email.ts`, refactored to a low-level `sendEmail` + `resolveAdminAlertEmails()` — `ADMIN_ALERT_EMAILS` override, else ADMIN users' addresses minus `@example.com`) and `sendTelegram()` (`src/lib/telegram.ts` — Bot API, `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID`, nullable). Both run inside `after()` (post-response, can't slow or break the student's submit) wrapped in `Promise.allSettled`. Email is dead until Brevo activates; Telegram needs the owner to make a bot (@BotFather) + set the two env vars. `tests/setup.ts` mocks `next/server`'s `after()` to run inline. `tsc` + `eslint` + `next build` + `vitest` (27/27) clean.
- **Performance pass (2026-09-07)**: (1) **indexes** — `QuizAttempt` had none, so the student dashboard's pass-count, the admin metrics aggregate, and `/api/quizzes/[id]/attempts` were sequential scans that degrade as attempts accumulate; added `@@index([userId])` + `@@index([quizId])`, plus `Question([quizId])`, `Enrollment([courseId])`, `LessonProgress([lessonId])` (the `userId`-leading unique indexes already cover `where:{userId}`). **On the `test` branch; still needs `prisma db push` to prod.** (2) `getSiteSettings()` — was an uncached DB read on every page (marketing + every `force-dynamic` dashboard/lesson render); now a 60 s per-instance TTL cache in `src/lib/site-settings.ts`, busted by `PATCH /api/settings`. (3) `GET /api/notifications` — dropped the separate unread `count()` (the bell polls this per user); unread is derived from the 20 fetched rows, and the poll interval went 60 s → 90 s (a window `focus` refetch already covers tab-return). (4) student dashboard — the enrollments query used a deep `include` that pulled every `Lesson.contentHtml` just to count lessons; now a narrow `select`; `quizAttempt.findMany` → `count()`; `select` on the progress query. `tsc` + `eslint` + `next build` + `vitest` (27/27) clean. Capacity context: on the free Neon plan the platform comfortably serves a few hundred concurrent active students; the hard ceiling is Neon free's ~192 compute-hours/month, which sustained real traffic burns in ~a week — a real launch needs Neon Launch ($19) + Vercel Pro ($20, also required for commercial use).
- **Staging privacy gate (2026-09-07)**: `src/middleware.ts` gained an opt-in `STAGING_PASSWORD` gate — inert unless the env var is set (same nullable-config pattern as email/Upstash/storage), so local dev and a real production deploy are untouched. When set, the matcher widens to the whole site: page routes get a small Arabic RTL navy/gold password form, `/api/*` gets a 401 JSON, and every response carries `X-Robots-Tag: noindex, nofollow`. Submitting the password at `/staging-unlock` stores a SHA-256 token in an httpOnly `staging_gate` cookie (30 d); the NextAuth `withAuth` dashboard logic is composed in unchanged (verified `/dashboard/*` still redirects unauthenticated users to sign-in). `tsc` + `eslint` + `next build` clean; tested locally against `next start` (gate page, API 401, wrong/right password, cookie passthrough). Needs `STAGING_PASSWORD` actually set on Vercel + a redeploy to take effect; unset it at launch. (Next 16 also prints a deprecation notice suggesting `middleware.ts` → `proxy.ts` — pre-existing, not addressed here.)

## 4. Pending / Known Gaps

- **Email OTP provider is Brevo — set up, but Brevo hasn't enabled transactional sending**: provider history is Resend → SendGrid (never activated) → **Brevo (2026-09-07)**. `src/lib/email.ts` POSTs Brevo's transactional HTTP API directly (no SDK; `@sendgrid/mail` uninstalled). Commit `9a09eee`, deployed to Production. Brevo account created 2026-09-07, two senders verified (`mohamed.hussien.academy@gmail.com` in use, `omarhamuda20@gmail.com`), API key generated, `BREVO_API_KEY` / `EMAIL_FROM` / `EMAIL_FROM_NAME` set in `.env.local` **and** on Vercel (Production), redeployed. Verified end-to-end that the live site authenticates with Brevo (calls show in Brevo → Transactional → Logs) and one early test mail reached Gmail. **Blocker:** Brevo shows *"Your sending platform is currently disabled"* — new Brevo accounts need transactional sending **activated by Brevo support** (help.brevo.com → submit a request); not a config error on our side. **Still needed from the user:** open that support ticket (low-volume OTP, <100/day, verified sender). Once activated, email works with no further changes. Fallback if Brevo stalls: Gmail SMTP via `nodemailer` + a Google App Password (~15 min code swap) — owner opted to wait on Brevo instead. Until it works, `admin:recover` / dev OTP log get accounts in.
- **Prepaid codes: no revoke/delete or CSV export**: the admin can generate and view codes and copy them (per-row or the fresh batch's "copy-all"), but there's no UI to delete/disable an unused code or export the full list as a file. Add a `DELETE /api/prepaid-codes/[id]` (unused-only) + a CSV download if the lecturer needs either.
- **Code-order flow: no email, no student cancel**: in-app notifications now exist (bell in the dashboard header, fired on approve/reject/new-request), but there's no **email** (the email provider isn't activated yet — see the Brevo entry above) — a student not logged in won't know their request was answered until they return. A student also can't cancel their own PENDING request. Both are small adds.
- **Lesson-player double sidebar**: `/dashboard/student/courses/[slug]/[lessonId]` renders both the role-based `DashboardShell` sidebar and the course-navigation `LessonSidebar` — functional but visually redundant on desktop. Would need restructuring the route outside the `dashboard/student` layout tree to fix properly. *(Phase 3 of the UI overhaul, 2026-09-07, made `LessonSidebar` `hidden lg:block` + added a `<details>` mobile nav, so the phone case is fixed; the desktop double-sidebar redundancy remains.)*
- **No "delete quiz" UI**: `DELETE /api/quizzes/[id]` exists (admin-only) but nothing in the admin UI calls it — only individual questions can be deleted via the UI.
- **Test/sample video**: all lesson `videoUrl`s point to a public Google Cloud sample clip (`ForBiggerBlazes.mp4`, ~15s, Creative Commons) as a placeholder — needs real course footage before launch.
- **No online payment gateway — by decision, not just a blocker (2026-09-07).** Background: Stripe doesn't support Egypt-based merchants (only ~46 countries; the only Stripe route is a US LLC via Stripe Atlas — $500 + $100/yr + Delaware franchise tax + repatriating USD). Egypt-native gateways (**Paymob** ~2.75%+3 EGP/txn, **Fawry** ~1.5–2.5%) both need KYC business docs, and the owner has no registered business yet (a sole proprietorship / منشأة فردية would be the lightweight path — a government step only the owner can do). Rather than wait on that, the platform **dropped online payment entirely**: Stripe SDK/route/webhook/keys all removed, and access is code-based. Money is collected manually (Vodafone Cash / InstaPay / bank / cash) and access is granted by (a) a prepaid code, or (b) the lecturer approving a `CodeOrder` request. **If a gateway is ever wanted later**, the clean insertion point is auto-approving a `CodeOrder` from a Paymob/Fawry webhook — the request→grant plumbing already exists.
- **No real imagery anywhere**: `public/` still only has the default Next.js starter SVGs — no course thumbnails (the `Course.thumbnailUrl` field now works end-to-end via the admin edit form, but no admin has set one yet), no instructor photo (still a generic `<User>` icon in a gradient circle standing in for محمد حسين). Needs real photos/thumbnails supplied by the user — can't be fabricated.
- **No module/lesson reordering UI**: `order` fields exist on `Module`/`Lesson` and are respected everywhere they're read, but there's still no drag/reorder or move-up/move-down control — new items always append at the end. Module create/delete now exists (this session); reordering does not.
- **User role management: DONE 2026-09-07** — `/dashboard/admin/users` has per-row actions: promote/demote STUDENT↔ADMIN and disable/enable (`PATCH /api/users/[id]`, `UserRowActions.tsx`). Guards: can't edit your own row (400), can't demote/disable the last active ADMIN (409), audit-logged (`user.role_change` / `user.disable` / `user.enable`). New `User.disabledAt` column (**pushed to `test`; prod still needs `prisma db push`**); a disabled user is blocked at login in `authOptions.authorize` (an existing JWT session persists until expiry — accepted limitation). Tests: `tests/api/users-manage.test.ts`. **Still not built:** manual-enroll-a-user (deliberately deferred — it's a third enrollment-granting path and needs its own pass against the enrollment invariant).
- **No search/filter on the `users` / `courses` admin lists** (the `progress` list got search + column sort in Phase 2 of the UI overhaul — 2026-09-07; `users` and `courses` are still server-paginated only) — fine at current scale, will need it as the user base grows.
- **Upstash Redis rate limiting is code-complete but unverified end-to-end** — no real Upstash account/credentials exist yet (free tier, no card needed). Currently running on the tested in-memory fallback. See CLAUDE.md's "Performance" section.

## 5. UI/UX Overhaul — Phases 1–3 complete

On 2026-09-06 the user requested a large, explicit UI/UX upgrade across both dashboards, using shadcn/ui + framer-motion + Recharts on top of the existing Next.js/Tailwind/dark-navy-gold stack, one phase at a time. Phases 1–3 are now done. Full detail per phase is in section 3; this is the scope-vs-status map.

**Requested scope** (from the user's own spec, verbatim intent preserved):
1. **Visual upgrades**: course cards with hover effects + circular progress rings — ✅ student course cards (Phase 1). Admin dashboard charts (Recharts, enrollments/activity) — ✅ Phase 2 (8-week activity area chart + enrollments-per-course bar chart + 6 animated stat cards).
2. **Interactions & animations**: page transitions, staggered list animations, toasts — ✅ staggered lists + hover (Phase 1 student course list, Phase 2 admin stat cards + table rows); ✅ route-to-route page-transition fade (Phase 3, `app/dashboard/template.tsx`, opacity-only, reduced-motion aware); sonner toasts pre-existed.
3. **New features**:
   - Student: gamification widget (streaks/badges) — ✅ Phase 1 (real data). Dynamic sliding sidebar in the video player for lesson resources/tools — ✅ Phase 3 (`LessonWorkspacePanel.tsx`).
   - Admin: Quick Actions dropdown — ✅ Phase 2 (`QuickActions.tsx`), wired to navigation. Upgraded student table with sorting/filtering — ✅ Phase 2 (`StudentProgressTable.tsx`). **Bulk actions — not done** (overlaps the open "No user role management UI" gap; grants ADMIN, needs a security review).
   - The spec's "تنبيه للطلاب" broadcast — ✅ done as the standalone **admin→student announcement banner** (2026-09-07, section 3).
4. **Responsive polish** for the new nav/charts/interactive elements — ✅ Phase 3 mobile pass (lesson-player `LessonSidebar` → `hidden lg:block` + `<details>` mobile nav, `QuickActions` re-anchored, chart axes thinned). Still **not verified on a real device/viewport** — no browser/device tool in any of these sessions; this was a code-level pass.

**All new files**: `CircularProgress.tsx`, `GamificationWidget.tsx`, `StudentCourseList.tsx`, `AnnouncementBanner.tsx`, `LessonWorkspacePanel.tsx` (`src/components/dashboard/` + `src/components/course/`); `src/lib/admin-metrics.ts`, `src/lib/gamification.ts`; `AdminStatCards.tsx`, `AdminCharts.tsx`, `QuickActions.tsx`, `StudentProgressTable.tsx` (`src/components/admin/`); `src/app/dashboard/template.tsx`. Deleted: `src/components/course/ResourceList.tsx`.

**Not started / deferred**:
- **Real device / screen-reader / mobile-viewport testing** of everything above — needs a browser or device tool that hasn't been available.
- **Bulk/role actions** on the student table (see "No user role management UI" in section 4).
- Per-user **notification feed** (new `Notification` model, bell + unread count, polling) — the documented next tier past the announcement banner, only worth building when a targeted or event-driven use case appears.
